import {createSubModel}from '../../models/subModels/index.js';
import {}from '../../middleWares/index.js';
export const createSubController=async(req,res,next)=>{
     const {userId,taskId}
   //already submitted userId and taskid matching in submission table or check the count to a n
   //in application accepted application.status=ACCEPTED

}
